#include "main.h"

/**
 * _abs -> check if the character is alphabet
 * @n: a charaacter argument
 * Return: return  if  lower and 0 is nnot
 */

int _abs(int n)
{
	if (n < 0)
		n = -n;
	return (n)
}
